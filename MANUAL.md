# FET Studio 이용 방법

Keithley 측정 파일을 올리면 transfer / output curve 를 스스로 구분해 그래프를 그리고
성능 지표와 진단을 계산한다. 계산에 쓰인 모든 수식·상수·판정 기준은 **[분석 방법] 탭**에
있다 — 이 문서는 수식을 반복하지 않고 "분석 방법 탭 참조"로만 안내한다.

## 1. 파일 형식과 자동 인식

### 1.1 지원 형식

Keithley 4200-SCS / KTEI 가 내보낸 `.xls` (구형 OLE2) 및 `.xlsx`. 시트 `Data` 와
`Settings` 를 읽는다. 여러 엔진(기본 → xlrd → openpyxl → HTML 표 → 탭 구분 텍스트)을
순서대로 시도하므로 파일이 살짝 손상돼 있어도 대부분 열린다.

### 1.2 커브 종류 판정 — 3단 폴백

파일 이름을 특정 규칙에 맞출 필요가 없다. 아래 순서로 자동 판정하고, 어느 단계에서
결정됐는지를 소자 카드에 근거로 표시한다.

1. **`Settings` 의 Forcing Function 행** (가장 신뢰도 높음)
   - Gate = `Voltage Sweep` 이고 Drain = `Voltage Bias` → **transfer**
   - Gate = `Voltage Step` 이고 Drain = `Voltage Sweep` → **output**
2. **`Data` 열 구조** (Settings 를 못 읽었을 때)
   - `GateV` 열이 있고 `DrainV` 로 시작하는 열이 없다 → transfer
   - `GateI(n)/GateV(n)/DrainI(n)/DrainV(n)` 형태의 4열 블록이 반복되고, 각 블록의
     `GateV(n)` 이 (그 블록 안에서) 상수다 → output
3. **`Test Name` 과 파일명** (최후 수단)
   - `Test Name` 과 파일명을 합쳐 영숫자 이외 문자로 토큰을 나눈 뒤, `out` 또는
     `output` 토큰이 있으면 output, 없으면 transfer 로 본다. 즉 파일명에 "transfer"
     라고 적어도 판정에는 영향이 없다 — out/output 이 없으면 기본값이 transfer 다.

3단계까지 내려가면(=Settings 와 열 구조 모두로 판정하지 못하면) 소자 카드에 "파일명으로
판정했다"는 경고가 뜬다. 잘못 판정됐다면 소자 패널에서 직접 바꿀 수 있다.

### 1.3 dual sweep 분리

`Settings` 의 `Dual Sweep Mode = Enabled` 면 데이터를 forward/reverse 로 나눈다.

1순위: `Settings` 의 `Number of Points` 절반 지점. (단, 그 값이 실제 데이터 길이와
±1 이내로 맞을 때만 신뢰한다.)
2순위: 못 읽거나 값이 안 맞으면 전압 스윕 방향의 부호가 뒤집히는 지점(turning point)을
찾는다. turning point 에서는 같은 전압이 두 번 연속 나올 수 있어, 인접 차분의 부호
변화만 보면 실패하는 경우가 있기 때문에 1순위를 우선한다.
둘 다 실패하면 데이터 길이의 절반에서 자른다.

Gate 스윕(transfer)과 Drain 스윕(output)은 각각 자신의 dual sweep 설정을 따로 읽는다.

### 1.4 한 파일에 측정이 여러 번 있을 때

같은 조건을 다시 측정하면 KTEI 는 데이터 시트를 `Data`, `Append1`, `Append2` … 로
늘리고, `Settings` 에도 런마다 블록을 하나씩 붙인다. Settings 블록 헤더가
**`Initial Run`** 이면 `Data` 시트, **`Append N`** 이면 `AppendN` 시트(예: `Append1`)에
대응하며, 헤더 옆에 **`Latest Run`** 표시가 붙은 블록이 가장 최근 측정이다.

앱은 **모든 런을 읽어 보존**하고, 소자 패널의 "측정 런" 드롭다운에서 고를 수 있게 한다
(런이 하나뿐인 파일에는 드롭다운이 뜨지 않는다). 기본 선택은 Latest Run 이다. 각 런은
**자기 블록의 설정**(V_DS, dual sweep 여부, Number of Points 등)을 그대로 쓰므로,
런을 합쳐서 읽지 않는 한 데이터와 설정이 어긋나지 않는다.

예: `Example/1-3 best.xls` 는 `Data`(15:34:54, Initial Run) 와 `Append1`(15:35:10,
Latest Run) 두 번의 transfer 측정을 담고 있다. 드롭다운을 열지 않으면 기본으로
`Append1` 이 분석된다. 예제 9개 소자 중 이렇게 재측정이 들어 있는 파일은 이 하나뿐이고,
나머지 파일은 모두 단일 런이다.

### 1.5 소자 자동 그룹핑

파일명에서 확장자와 접미 토큰(`out` / `output` / `transfer` / `tr` / `best`, 공백·언더바
구분)을 떼어낸 나머지가 소자 이름(stem)이다. 예: `1-3 best.xls` 와 `1-3 out.xls` → 소자
`1-3`. `1-1.xls` 와 `1-1 out.xls` → 소자 `1-1`.

같은 종류(transfer 또는 output)의 **파일**이 한 소자에 두 개 이상 들어오면 먼저 들어온
파일만 쓰고 나머지는 "미사용" 목록에 보관한다 — 소자 패널에서 교체할 수 있다. (한 파일
안에 여러 측정 런이 있는 것은 이와 별개로 §1.4 규칙에 따라 전부 보존된다.)

## 2. 소자 파라미터 입력

좌측 편집 패널에서 채널 폭 **W** (µm), 채널 길이 **L** (µm), 유전체 상수 **ε_r**, 유전체
두께 **d** (nm) 를 입력한다. 유전체는 SiO₂ · Al₂O₃ · HfO₂ · PMMA 프리셋 중 고르거나
Custom 으로 ε_r 을 직접 넣을 수 있다(프리셋 값은 [분석 방법] 탭 §1 참조).

값을 비워두면 하단 [전역 기본값] 에 입력한 값을 상속한다. 웨이퍼 한 장을 통째로 올렸고
소자 파라미터가 동일하면 전역값만 한 번 채우면 되고, 특정 소자만 다르면 그 소자 패널에서
덮어쓴다.

W / L / ε_r / d 네 값이 모두 채워지면 `C_ox` 가 자동 계산돼 패널에 뜬다. 하나라도 비면
`mu_sat` 은 계산되지 않고 `V_th` 만 나온다. 계산식은 [분석 방법] 탭 §1, §2 참조.

`V_DS` 는 파일의 `Settings` 에서 자동으로 읽으므로 따로 입력할 필요가 없다. 선택한
측정 런이 바뀌면 그 런의 `V_DS` 로 다시 계산된다.

## 3. Fit 구간

기본은 자동 탐색이다. √|I_D| 가 가장 곧게(R² 최대) 뻗은 구간을 알고리즘이 찾아 우측
Y축 위에 직선과 구간 음영으로 표시한다. 탐색 규칙(윈도우 크기, 동점 처리 등)은 [분석
방법] 탭 §2.2 에 그대로 적혀 있다.

자동 구간이 마음에 들지 않으면 [수동 지정] 을 켜고 V_G 범위를 숫자로 넣으면 자동
탐색이 꺼지고 그 구간으로 즉시 재계산된다. forward/reverse 는 각각 독립적으로 fit 되므로
필요하면 두 branch 의 구간을 다르게 지정할 수 있다.

## 4. 그래프 서식과 미리보기 배율

그래프는 논문 투고용으로 실측 10 × 8 inch(960 × 768 px, 96 dpi) 로 만들어진다. 화면
좌우 배치 폭이 이보다 좁으면 그대로는 다 들어가지 않으므로, **표시용으로만** figure
전체(크기·폰트·선 두께)에 배율을 곱해 축소해 보여준다. 이 배율은 자동으로도, [서식 ·
크기] 패널에서 수동(25–200 %)으로도 고를 수 있다.

**내보내는 파일은 항상 배율 1×(실측 크기)로 다시 만들어지므로 미리보기 배율의 영향을
받지 않는다.**

서식(색·선 두께·축 범위/스케일·Origin 방식 geometry·인셋 위치·폰트)은 [프리셋] 에서
JSON 으로 저장해 다른 소자에 그대로 적용할 수 있다. 소자 파라미터(W/L/ε_r/d)와 output
진단 임계값은 소자마다 다른 측정 조건/판정 기준이라 프리셋에 포함되지 않는다.

Transfer 그래프는 좌 Y축 `|I_D| (A)` (log), 우 Y축 `√|I_D| (A^0.5)` (linear) 이중 Y축이고,
Output 그래프는 V_G 순서대로 명도가 단조 감소하는 단색 그라데이션을 쓴다. 그래프 규약의
전체 근거(왜 절댓값 기호를 쓰는지 등)는 [분석 방법] 탭 §5 참조.

## 5. 내보내기

| 형식 | 배경 | 용도 |
|---|---|---|
| PNG | **투명** (`rgba(0,0,0,0)`) | PPT · 포스터에 얹기 |
| JPG | **흰색** (`#FFFFFF`, 알파 미지원) | 문서 · 메일 |
| SVG | 투명 (벡터) | Illustrator 재편집 |
| PDF | 흰색 (벡터) | 논문 투고 |

화면에 보이는 그래프는 항상 흰 배경이고, 배경 전환은 **내보내는 시점에만** figure
복제본에 적용된다 — 원본 화면 표시에는 영향을 주지 않는다.

배율 1× / 2× / 4× 를 고를 수 있다. 4× 면 3840 × 3072 px 가 된다.

[전체 ZIP] 은 소자별 폴더(예: `1-3/transfer.png`, `1-3/output.png`)에 그래프 이미지와
가공된 원데이터 CSV(transfer: V_G/I_G/I_D/√|I_D|/branch/fit 직선값, output:
V_G/branch/V_D/I_D/I_G) 를 담고, 소자 전체 요약표를 함께 넣는다. 요약표는 CSV(엑셀에서
한글이 깨지지 않도록 UTF-8 BOM 포함)와 XLSX 로 따로 받을 수 있으며, 개별 소자 다운로드도
가능하다.

**주의:** 투명 PNG 는 축·눈금·글자가 모두 검정이므로 어두운 배경의 슬라이드에 얹으면
보이지 않는다. 어두운 배경에는 흰 배경 JPG 를 쓰거나, SVG 를 열어 색을 직접 바꾼다.

## 6. 자주 겪는 문제

- **커브 종류가 잘못 잡혔다** — 소자 패널에 판정 근거(forcing / structure / name)가
  뜬다. "파일명으로 판정" 이라고 나오면 `Settings` 를 못 읽었거나 열 구조로도 판정하지
  못한 것이니 원본 파일의 `Settings` 시트를 확인한다.
- **`mu_sat` 이 안 나온다** — W / L / ε_r / d 중 빈 항목이 있다는 뜻이다. 전역 기본값을
  채우거나 그 소자 패널에서 직접 입력한다.
- **fit R² 가 낮다는 경고가 뜬다** — 자동 구간이 서브스레숄드 영역까지 끌고 들어간
  경우가 흔하다. [수동 지정] 으로 포화 영역만 지정한다.
- **소자가 잘못 묶였다** — 파일명 stem(§1.5) 기준이라 접미 토큰이 예상 밖이면 다른
  소자로 갈릴 수 있다. 소자 리스트에서 그룹을 확인하고, 필요하면 파일명을 정리한 뒤
  다시 올린다.
- **재측정 런을 잘못 골랐다** — 소자 패널의 "측정 런" 드롭다운에서 `Data` / `AppendN`
  을 바꿔 고른다. 기본값은 항상 Latest Run 이다.
- **이미지 내보내기가 실패한다** — 서버에 렌더러(kaleido/Chromium)가 없는 경우다.
  자동으로 HTML 로 대체 저장되니 브라우저에서 열어 인쇄하거나 PDF 로 저장하면 된다.
