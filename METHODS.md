# FET Studio 분석 방법

이 문서는 앱이 계산하는 **모든 수식·물리상수·알고리즘 상수·판정 임계값·가정과 한계**를 담는다.
앱은 이 파일을 그대로 읽어 [분석 방법] 탭에 렌더하므로 문서와 실제 계산이 어긋날 수 없다.
값이 연구실 관례와 다르면 이 문서를 근거로 `fet_app/constants.py` 수정을 요청하면 된다.

사용하는 기호는 다음과 같다. 코드의 필드명·변수명과 그대로 맞춰 두었다.

| 코드 표기 | 의미 | 단위 |
|---|---|---|
| `C_ox` | 산화막(유전체) 정전용량 | F/cm² |
| `mu_sat` (μ_sat) | 포화 이동도 | cm² V⁻¹ s⁻¹ |
| `V_th` | 문턱 전압 | V |
| `I_on`, `I_off` | on/off 전류 | A |
| `SS` | subthreshold swing | mV/dec |
| `ΔV_th` | 히스테리시스 (reverse − forward) | V |
| `I_drive` | 소자의 온상태 구동전류 (output 진단 정규화 분모) | A |

## 1. 소자 파라미터

| 기호 | 의미 | UI 단위 | 내부 단위 | 환산 |
|---|---|---|---|---|
| W | 채널 폭 | µm | cm | ×1e-4 |
| L | 채널 길이 | µm | cm | ×1e-4 |
| ε_r | 유전상수 | — | — | — |
| d | 유전체 두께 | nm | cm | ×1e-7 |

네 값이 모두 채워져야 `C_ox` 와 `mu_sat` 이 계산된다(`DeviceParams.is_complete()`).
하나라도 비면 `V_th` 만 나오고 `mu_sat` 은 `None` 이다.

### 1.1 산화막 정전용량 C_ox

$$
C_{ox} = \frac{\varepsilon_0 \, \varepsilon_r}{d} \quad [\text{F/cm}^2]
$$

$$
\varepsilon_0 = 8.854 \times 10^{-14}\ \text{F/cm} \quad (\text{진공 유전율, cm 단위계})
$$

여기서 `d` 는 UI 에 nm 로 입력되고 내부에서 ×1e-7 로 cm 환산된 값이다. 코드 상수는
`EPSILON_0 = 8.854e-14` 이며, 정확도를 위해 반올림하지 않고 그대로 쓴다.

유전체 프리셋 ε_r:

| 프리셋 | ε_r |
|---|---|
| SiO₂ | 3.9 |
| Al₂O₃ | 9.0 |
| HfO₂ | 25.0 |
| PMMA | 3.6 |
| Custom | 직접 입력 |

**검산**: SiO₂ 300 nm → `C_ox = 3.9 × 8.854e-14 / 300e-7 = 1.151e-8 F/cm² = 11.51 nF/cm²`.
직접 계산한 값과 앱이 표시하는 값이 다르면 단위 환산(µm→cm ×1e-4, nm→cm ×1e-7)부터 의심한다.

## 2. Transfer 지표

### 2.1 포화 이동도 μ_sat 과 문턱 전압 V_th

포화영역 제곱법칙을 가정한다.

$$
I_D = \frac{W}{2L}\, \mu_{sat}\, C_{ox}\, (V_G - V_{th})^2
$$

양변 제곱근을 취하면 √|I_D| 가 V_G 에 대해 직선이 된다. 이 직선을 forward branch 에서
최소자승 fit 해 `√|I_D| = m·V_G + b` (기울기 m, 절편 b) 를 얻으면:

$$
\mu_{sat} = \frac{2L}{W\, C_{ox}}\, m^2 \qquad [\text{cm}^2\,\text{V}^{-1}\,\text{s}^{-1}]
$$

$$
V_{th} = -\frac{b}{m} \qquad [\text{V}]
$$

이 소자는 p-type 이라 m < 0 이지만 m² 을 쓰므로 `mu_sat` 은 항상 양수로 나온다.
함께 보고하는 값은 fit 의 R², 사용된 V_G 구간, 구간 내 점 개수다. reverse branch 가
있으면 §2.2 의 알고리즘을 독립적으로 다시 적용해 `V_th(reverse)` 도 구한다(§2.5 참조).

fit 의 R² 가 `FIT_R2_WARN = 0.99` 미만이면 "fit R² 가 낮습니다" 경고를 띄운다 — 자동
구간이 서브스레숄드 영역까지 끌고 들어갔을 가능성이 크다는 신호다.

**포화 조건 검사.** fit 구간 전체에서 `|V_DS| ≥ |V_G − V_th|` 가 성립해야 제곱법칙이
유효하다. fit 구간의 양 끝값 중 `V_th` 에서 더 먼 쪽 `|V_G − V_th|` 가 `|V_DS|` 를
넘으면 "포화 조건이 깨진다"는 경고를 띄우고 `mu_sat` 이 과대평가될 수 있음을 알린다.

### 2.2 fit 구간 자동 탐색

대상은 **forward branch 의 √|I_D| vs V_G**. 알고리즘:

1. off 바닥 `I_off = min|I_D|` (정확히 0인 점은 제외) 를 구한다.
2. 후보 영역 = `|I_D| > 100 × I_off` 인 **최장 연속 구간**.
3. 윈도우 크기를 **최소 10 점**부터 **후보 영역의 60 %** 까지 1점씩 키우며 슬라이딩하고,
   각 윈도우에서 최소자승 fit 의 R² 를 계산한다.
4. R² 최댓값을 주는 윈도우를 선택한다. 두 윈도우의 R² 차이가 `5e-4`(=0.0005) 이내면
   **동점으로 보고 점이 많은 쪽**을 택한다(짧고 우연히 R² 가 조금 높은 창에 밀리지
   않도록).

숫자로 V_G 범위를 직접 지정하면 자동 탐색은 꺼지고 그 구간으로 즉시 재계산한다
([이용 방법] §3 참조). reverse branch 도 같은 알고리즘을 독립 적용한다.

### 2.3 On/Off 전류비

$$
I_{on}/I_{off} = \frac{\max|I_D|}{\min|I_D|} \qquad (\text{forward branch 전 구간, } I_D=0 \text{ 제외})
$$

스윕 범위에 의존하는 값이라 §4 에서 다시 언급하듯 다른 범위로 측정한 소자와 직접
비교해서는 안 된다.

### 2.4 Subthreshold swing SS

$$
SS = \min\!\left(\frac{dV_G}{d\log_{10}|I_D|}\right) \quad [\text{V/dec}] \;\rightarrow\; \times 1000 \;\rightarrow\; [\text{mV/dec}]
$$

구현은 수치적으로 동치인 식을 쓴다:

$$
SS = \frac{1000}{\max\left|\dfrac{d\log_{10}|I_D|}{dV_G}\right|} \qquad [\text{mV/dec}]
$$

즉 국소 기울기 `d log₁₀|I_D| / dV_G` 의 절댓값이 최대인 지점을 찾아 그 역수를 취하는
것이므로 결과는 위 정의와 동일하다. 국소 기울기는 **5 점 이동 최소자승 회귀**(`SS_WINDOW = 5`)
로 구하고, 탐색 범위는 `I_off × 10` 이상 `I_on / 10` 이하인 서브스레숄드 구간으로 제한한다.

### 2.5 히스테리시스 ΔV_th

$$
\Delta V_{th} = V_{th}(\text{reverse}) - V_{th}(\text{forward}) \qquad [\text{V}]
$$

reverse branch 는 §2.2 와 완전히 같은 방식(같은 상수, 같은 알고리즘)으로 forward 와
독립적으로 fit 한다. forward 의 fit 결과를 reverse 에 그대로 적용하지 않는다.

## 3. Output 진단

각 V_G 블록마다 4종 진단을 계산하고, 소자 전체 대표값은 블록 중 **나쁜 쪽**을 취한다
(비율 3종은 최댓값, 원점 선형성 R² 는 최솟값). 임계값은 UI 에서 조정할 수 있으며 아래는
`DEFAULT_THRESHOLDS` 의 기본값이다.

### 3.1 정규화 기준 — 왜 블록 자신의 최댓값으로 나누지 않는가

개발 중 이 판단으로 오경보를 냈다가 고쳤다: 처음에는 각 항목을 **그 블록 안의**
`max|I_D|` 로 나눴다. `V_G = 0 V` 블록은 소자가 꺼져 있어 전류가 노이즈 수준(예제에서
~1 nA)인데, 그 블록 자신의 최댓값(역시 노이즈)으로 나누면 노이즈끼리 나눈 값이 나와
비율이 폭주한다. 실제로 `1-1 out.xls` 의 V_G = 0 블록에서 0 V 오프셋 57.89 %, 게이트
누설 100.67 % 로 계산되어, 멀쩡한 소자가 불량으로 찍혔다. 예제 9개 소자 중 **6개**가 이
이유만으로 오경보였다.

그래서 각 블록의 비율을 그 블록 자신이 아니라 **소자 전체의 구동전류**로 정규화한다:

$$
I_{drive} = \max_{\text{모든 블록}} \big(\max|I_D|\big) \qquad (\text{소자 온상태 구동전류})
$$

$$
\text{on-block} \iff \max|I_D|_{\text{그 블록}} \;\ge\; 0.01 \times I_{drive} \quad (=1\%,\ \text{켜진 블록})
$$

`I_drive` 로 나누면 꺼진 블록의 노이즈는 (분모가 커서) 작은 값으로 남고, 곡선 **모양**
판정(원점 선형성·포화 도달)은 애초에 on-block 에서만 실행하므로 노이즈 개형이 판정
대상에 들어오지 않는다.

### 3.2 진단 4종

| 항목 | 정의 | 대상 | 기본 임계 | 의미 |
|---|---|---|---|---|
| 0 V 오프셋 | `\|I_D(V_D=0)\| / I_drive` | 전 블록 | > 1 % | 원점에서 출발하지 않음 |
| 원점 선형성 | `\|V_D\| ≤ 스윕폭의 10 %` 구간 선형 fit R² | **켜진 블록만** | < 0.99 | S자 개형 = 컨택트 저항 / Schottky 장벽 |
| 포화 도달 | `(dI_D/dV_D)_말단 / (dI_D/dV_D)_원점` | **켜진 블록만** | > 0.1 | 미포화 |
| 게이트 누설 | `max\|I_G\| / I_drive` | 전 블록 | > 1 % | 게이트 누설 과다 |

- 말단·원점 기울기는 각각 양 끝 **5 점**(`DIAG_SLOPE_POINTS = 5`)의 선형 회귀 기울기다.
- 꺼진 블록(off-block)의 **원점 선형성·포화 도달은 계산하지 않고 경고도 달지 않는다.**
  꺼진 소자의 곡선 개형은 노이즈이므로 판정 대상이 아니다. UI 에는 "off (진단 생략)"
  으로 표시된다.
- 꺼진 블록에서도 **0 V 오프셋과 게이트 누설은 계산한다.** 꺼진 상태에서의 큰 누설은
  실제 결함이고, `I_drive` 로 나누므로(§3.1) 노이즈 때문에 값이 부풀지 않는다.
- 소자 대표값 집계는 나쁜 쪽을 취한다: 비율 3종(0 V 오프셋·포화·게이트 누설)은
  블록 중 `max`, 원점 선형성 R² 는 `min`.

## 4. 가정과 한계

- `mu_sat` 은 **포화영역 제곱법칙**(§2.1)을 가정한다. 접촉저항이 크거나 이동도가
  게이트 전압에 의존하는 소자에서는 **과소평가**된다.
- 포화 조건 `|V_DS| ≥ |V_G − V_th|` 가 fit 구간 전체에서 성립해야 한다. 깨지는 구간이
  fit 에 포함되면 경고가 뜨고, 반대 방향으로 `mu_sat` 이 **과대평가**될 수 있다.
- `SS` 는 측정 점(V_G) 간격보다 가파른 소자에서는 실제보다 크게(둔하게) 나온다 — 두
  샘플링 점 사이의 변화는 원리적으로 분해할 수 없기 때문이다. 예제 데이터는 V_G 간격이
  1 V 이고, 그 결과 계산된 `SS` 는 소자에 따라 약 2,100 ~ 3,500 mV/dec 로 큰 편이다.
  이보다 가파른 실제 SS 를 가진 소자라도 이 데이터로는 그 값을 확인할 수 없다.
- `I_on/I_off`(§2.3) 는 스윕 범위에 의존한다. 스윕 범위가 다른 두 소자의 `I_on/I_off`
  를 직접 비교하면 안 된다.
- 극성(n-type/p-type)은 자동 판별하지 않는다. 이 앱의 그래프·부호 관례(§5)는 p-type
  을 가정하며, p-type 이 아닌 데이터가 들어와도 계산 자체는 진행하되 경고를 띄운다.

## 5. 그래프 규약

- 논문용 흰 배경, 4면 박스 mirror ticks, ticks inside, 그리드 없음.
- 눈금 지수 표기는 항상 `1E-11` 형식(`exponentformat="E"`).
- 크기는 Origin 방식 2단계: Background(inch, 기본 10 × 8) → Graph(% of background,
  기본 Left 17.9 / Top 11.58 / Width 68.2 / Height 71.77).
- **Transfer**: 좌 Y축 `|I_D| (A)` (log), 우 Y축 `√|I_D| (A^0.5)` (linear), X축 `V_G (V)`.
  forward = 실선, reverse = 파선(같은 색). 우축 위에 fit 직선·fit 구간 음영·V_th 절편
  마커를 오버레이한다.
  - 좌축 제목에 절댓값 기호(`|I_D|`)를 쓰는 것은 **의도적**이다. 이 소자는 p-type 이라
    I_D 가 음수인데, log 축은 음수를 그릴 수 없으므로 |I_D| 를 그리는 것이 FET 문헌의
    일반적인 표기다. (같은 프로젝트군의 다른 앱은 절댓값 기호를 금지하는 규약을 쓰지만,
    다루는 데이터 극성이 달라 이 앱에서는 반대로 간다.)
- **Output**: X축 `V_D (V)`, Y축 `I_D (A)` 모두 linear. V_G 순서대로 명도가 단조
  감소하는 단색(base color) 그라데이션을 쓴다 — 흑백 인쇄나 색약 조건에서도 곡선의
  순서가 남도록 하기 위함이다. forward = 실선, reverse = 파선.
